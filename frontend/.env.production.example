# Production frontend reference only. Set this value in the Vercel project.
# It is public and embedded into the browser bundle during the build.
NEXT_PUBLIC_ACRA_API_BASE_URL=https://api.example.com/api/v1
